fizzbuzz(num) {
  if zero? (num % 15)
    then "FizzBuzz"
  else if zero? (num % 3)
    then "Fizz"
  else if zero? (num % 5)
    then "Buzz"
  else
    string num
}

(range 1 100)
  |> each (print (fizzbuzz _))
