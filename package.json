{
  "scripts": {
    "build": "clj -M -m cljs.main --target node --output-to dist/main.js -c netrine.cljs.core",
    "start": "npm run build && node dist/main",
    "release": "clj -M -m cljs.main --target node --output-to dist/main.js --optimizations advanced -c netrine.cljs.core"
  },
  "dependencies": {
    "source-map-support": "^0.5.19"
  }
}
